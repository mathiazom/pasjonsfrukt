<!DOCTYPE html>
<html lang="nb">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Podcast List</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 800px;
            margin: 0 auto;
            padding: 20px;
        }
        h1 {
            text-align: center;
            color: #2c3e50;
        }
        .podcast-list {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 20px;
        }
        .podcast-item {
            border: 1px solid #ddd;
            border-radius: 8px;
            padding: 15px;
            transition: transform 0.3s ease;
        }
        .podcast-item:hover {
            transform: translateY(-5px);
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
        }
        .podcast-item img {
            width: 100%;
            height: 200px;
            object-fit: cover;
            border-radius: 8px;
            margin-bottom: 10px;
        }
        .podcast-item h2 {
            margin-top: 0;
            color: #2c3e50;
        }
        .podcast-item p {
            margin-bottom: 10px;
        }
        .podcast-item a {
            display: inline-block;
            background-color: #3498db;
            color: white;
            padding: 8px 12px;
            text-decoration: none;
            border-radius: 4px;
            transition: background-color 0.3s ease;
        }
        .podcast-item a:hover {
            background-color: #2980b9;
        }
    </style>
</head>
<body>
    <h1>Podcaster fra PodMe</h1>
    <div class="podcast-list">
        {% for podcast in podcasts %}
        <div class="podcast-item">
            <img src="{{ podcast.image_url }}" alt="{{ podcast.title }}">
            <h2>{{ podcast.title }}</h2>
            <p>{{ podcast.description }}</p>
            <a href="{{ url.scheme }}://subscribeonandroid.com/{{ url.host }}{{ url.path }}{{ podcast.slug }}">Abonnere</a>
        </div>
        {% endfor %}
    </div>
</body>
</html>
